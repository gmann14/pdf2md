ACCEPTED → IMPLEMENTATION_PLAN.md item pdf2md#10 (fix PDF.js worker init under `next dev`)
---
id: fb-2026-06-20-001
project: pdf2md
source: e2e-finding
channel_ref: forge-e2e-gate
received: 2026-06-20T22:25:08Z
status: new
---

Conversion is broken in `next dev` (local dev server): uploading any PDF throws "Unexpected error: Object.defineProperty called on non-object" and shows the "Processing Error" state. The production static export (`pnpm build`) converts the SAME PDF correctly — verified by the new Playwright E2E suite, which passes against the export and failed against dev.

Root cause is almost certainly PDF.js worker initialization differing between `next dev` and the static export build. Look at packages/core/src/pdf-worker.ts (worker setup) and next.config.ts (webpack worker/alias handling). Fix so `pnpm dev` can convert — broken local dev blocks manual testing. Keep the existing E2E gate green (it runs against the prod export); optionally add a dev-mode smoke if cheap.
